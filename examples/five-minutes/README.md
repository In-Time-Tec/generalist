# Five minutes

Run the README quickstart with a scripted model and a SQLite Runtime that closes and reopens before awaiting the Run.

```bash
bun run --cwd packages/generalist build
bun run --cwd examples/five-minutes start
```
