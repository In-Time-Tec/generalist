# Use Effect AI vocabulary

Baton uses Effect AI `Prompt`, `Response`, `Tool`, `Toolkit`, `LanguageModel`, and `Chat` directly because a second payload or tool model would duplicate schemas and weaken composition with Effect providers. Baton adds only loop framing and services that Effect AI does not own.
