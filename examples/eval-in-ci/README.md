# Eval in CI

Run a deterministic no-credential smoke eval over `Agent.run`. CI typechecks this example; projects can also run it as a fast local acceptance check.

```bash
bun --cwd examples/eval-in-ci start
```
