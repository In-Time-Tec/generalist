# @tenetkit/mysql

MySQL runtime backend for TenetKit.

Use `layer(options)` to provide the Runtime services. Schema deployment stays separate through `RunSchema.plan`, `RunSchema.check`, and `RunSchema.apply`.
