{
  "name": "generalist-example-package-composition-guides",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun src/core.ts && bun src/providers.ts && bun src/instructions.ts && bun src/instructions-skills.ts && bun src/memory.ts && bun src/mcp.ts && bun src/transport.ts && bun src/foldkit.ts && bun src/test.ts",
    "typecheck": "bun --bun tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "catalog:",
    "@effect/platform-bun": "catalog:",
    "@smithy/fetch-http-handler": "catalog:",
    "effect": "catalog:",
    "generalist": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
