# Gemini via OpenAI Compatibility

Google AI Studio can be reached with the OpenAI-compatible helper from `@batonfx/providers` when using an OpenAI-compatible endpoint. Configure the provider in application code with Effect `Config` and keep live credentials out of examples.

Use the model catalog for static metadata overrides until first-party upstream Effect AI Google helpers are compatible with the pinned beta.
