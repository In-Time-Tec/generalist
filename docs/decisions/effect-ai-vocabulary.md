# Use Effect AI vocabulary

Generalist uses Effect AI `Prompt`, `Response`, `Tool`, `Toolkit`, `LanguageModel`, and `Chat` directly because a second payload or tool model would duplicate schemas and weaken composition with Effect providers. Generalist adds only loop framing and services that Effect AI does not own.
