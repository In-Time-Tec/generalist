# Memory Chat

Run two local agent turns with the same explicit memory key. The second turn receives working-memory recall without using a durable store or live embeddings.

```bash
bun --cwd examples/memory-chat start
```
