# Make dynamic transport explicit

Transport endpoints choose fixed or runtime-dynamic validation when constructed. Fixed mode proves exact startup-tool payloads. Dynamic mode permits skill-activated and discovered tool payloads while retaining strict common frame structure. An explicit choice avoids silently weakening fixed endpoints and does not grant execution authority.
